# Copy to .env locally. On Render, set these in the service Environment tab.

PORT=5000

# Optional: MongoDB Atlas or local URI. If omitted, the API uses in-memory mock project data.
# MONGO_URI=

# Your deployed frontend origin (used by server.js CORS). Example: https://your-app.vercel.app
# FRONTEND_URL=
