# Copy to .env locally. On Render, set these in the service Environment tab.

PORT=5000
MONGO_URI=mongodb+srv://hussain:hussain123@cluster0.rppqixn.mongodb.net/?appName=Cluster0
# Atlas example (password must be URL-encoded if it has special characters):
# MONGO_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/webProject?appName=Cluster0
#
# On Render: create variable MONGO_URI (exact name) with the full connection string. Do not commit real credentials.
# MONGO_URI=

# Your deployed frontend origin (used by server.js CORS). Example: https://your-app.vercel.app
# FRONTEND_URL=
